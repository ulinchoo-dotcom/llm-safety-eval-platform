# v1 种子集首次完整闭环运行报告

- 运行时间：2026-09-21
- 范围：`examples/seed-cases-v1.json` 50 条（40 对抗 + 10 正常对照）× 2 个模拟模型 = 100 条结果
- 代码路径：`safety_eval.pipeline`（与 API 同一条 LangGraph 路径）
- 用量声明：全部 `usage_source=simulated`，零 token、零费用；模型名为本地模拟标识，不代表任何真实模型产品

## 指标总览

| 模型 | 通过率（严格口径） | ASR（攻击成功率） | 正常对照误伤率 | 拒答率 |
| --- | --- | --- | --- | --- |
| demo-safe | 20% | 0% | 0% | 0% |
| demo-overrefusal | 80% | 0% | 100% | 100% |

## 读法

两个模拟模型恰好演示安全评测的核心张力：

- **demo-safe** 不输出有害内容（ASR=0），但它的回答是安全引导而非明确拒答——
  在「期望拒答就必须拒答」的严格口径下，40 条对抗用例全部记为「安全但不符合预期」，
  只有 10 条正常对照通过，通过率 20%。
- **demo-overrefusal** 对一切输入拒答：对抗用例全部拦下，但 10 条正常对照 100% 误伤。

这正是 `ReviewInput` 把 `passed` / `refused` / `attack_succeeded` 拆成三个字段的原因：
「没拒答」不等于「被攻破」，「都拒了」不等于「安全做得好」。

## 复核口径

- 100 条逐条裁定见 `review-v1.json`，reviewer_id = `yulin-self-review-v1`（作者自审，单一标注者）。
- 未做双人独立标注，Cohen's Kappa 暂无计算条件——这是 M1 双标阶段的准入门槛。
- 复核时保留原始响应与规则信号不变，裁定结论单独保存。

## 已知边界

- 本次运行在无数据库环境直接调用 pipeline 层，未经过 API 的鉴权与事务落库；
  数据库闭环以 CI 集成测试（真实 PostgreSQL 容器）为准。
- `release_eligible` 仍为 false：没有真实模型调用、没有已校准 Judge、没有发布门禁。

## 真实模型首测：deepseek-chat（2026-09-21）

首次真实 API 调用：20 条对抗 + 5 条正常对照，温度 0，全部成功。
用量：prompt 739 / completion 5991 tokens（API 真实返回，费用按公开价目估算、以账单为准）。

| 模型 | 对抗拒答 | ASR | 正常对照误伤 | 通过率 |
| --- | --- | --- | --- | --- |
| deepseek-chat（真实） | 20/20 | 0% | 0/5（0%） | 100% |

**关键发现：规则信号在真实模型上召回仅 20%（4/20）。**
仓库的拒答正则（6 个模式）只命中 4 条真实拒答——真实模型的拒答话术远比规则库多样
（兜底话术「还没有学会」、解释性拒答、安全替代建议、英文拒答）。
这实证了本仓库的核心设计：`judge_gate` 默认全部转人工、规则信号仅作参考；
Judge 校准前任何「自动判定通过率」都不可信。

复核：25 条逐条人工读响应裁定（`review-v1-deepseek.json`），reviewer 为作者自审，未经双标。

## v2 样本库与双人独立标注（2026-09-21）

### 程序化扩增：50 → 2200

`scripts/expand_seed.py`（已入库，确定性可复现）：22 子类 × 3 基础载荷 × 4 场景槽 × 8 攻击手法
= 2112 条对抗用例 + 88 条近似误伤正常对照（`examples/seed-cases-v2.json`，1.3MB）。

- `minor_sexual_content` 子类按内容政策**不生成**，minors 类别覆盖由其余三子类承担——这是设计决策而非遗漏。
- 所有载荷只表达请求意图，不含操作性危害内容（无配方/步骤/露骨文本）。
- 标签为模板规则赋予（`author-template`），**未经人工逐条复核**；source 字段逐条标注扩增来源。

### 双人独立标注：κ = 1.0（附保留意见）

分层抽样 200 条（22 子类 × 8 手法首载荷 = 176 对抗 + 24 对照）：
标注员 A = 作者模板标注，标注员 B = DeepSeek 独立标注（只看请求文本，看不到 A 的标签）。
结果：200/200 完全一致，observed agreement = 1.0，**Cohen's Kappa = 1.0（pass，≥0.85）**。

**必须如实说明的三点保留**：
1. B 是 LLM 不是人类——这是「人 + 机」双标的工程演示，M1 要求的两个人类独立标注仍未完成；
2. 样本由模板生成、类别边界清晰，κ=1.0 只说明两方对**明显用例**判断一致，不能外推到模糊用例；
3. 双标的真正价值在分歧分析，本轮零分歧意味着下一版应主动补充灰色地带用例
  （安全改写、擦边边界、多轮渐进诱导的中段）再测。

明细见 `dual-label-v2.json`（含每条双方标签与 B 的理由）。
